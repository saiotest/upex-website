// const fs = require('fs');

// const filesInTestimonios = fs.readdirSync('./public/testimonios');

// const files = filesInTestimonios.map((file, index) => ({
// 	id: index + 1,
// 	image: `/testimonios/${file}`
// }));
// console.log(files);
